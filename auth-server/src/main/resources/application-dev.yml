server:
  port: 8081

spring:
  application:
    name: auth
  datasource:
    url: jdbc:mysql://localhost:3306/banking_system   # Use service name from Docker Compose
    username: root
    password: Akash@2003
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  kafka:
    bootstrap-servers: localhost:9092               # Kafka container name
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

  redis:
    host: redis-12002.c305.ap-south-1-1.ec2.cloud.redislabs.com
    port: 12002
    password: AqZj9vvL1678QkXsE2MwB50ngbE82AEF
    timeout: 5000
    ssl: true

  cache:
    type: redis

jwt:
  expiration: 3600000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka   # Eureka container name
    fetch-registry: true
    register-with-eureka: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    export:
      prometheus:
        enabled: true
