server:
  port: 8083

spring:
  application:
    name: account

  datasource:
    url: jdbc:mysql://localhost:3306/banking_system   # MySQL container name
    username: root
    password: Akash@2003
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  cache:
    type: redis

  redis:
    host: redis-12002.c305.ap-south-1-1.ec2.cloud.redislabs.com
    port: 12002
    password: XyZ@AqZj9vvL1678QkXsE2MwB50ngbE82AEF
    timeout: 5000

    ssl: true   # 🔴 IMPORTANT for Redis Cloud

    cache:
      type: redis


  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: account-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer



eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka    # Eureka container name
    fetch-registry: true
    register-with-eureka: true

jwt:
  secret: U2VjdXJlS2V5Rm9yQmFua2luZ0FwcFNob3VsZEJlQmFzZTY0
  expiration: 3600000

management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    export:
      prometheus:
        enabled: true


