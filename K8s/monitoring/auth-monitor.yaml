apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: auth-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: auth
  namespaceSelector:
    matchNames:
      - vasta-bank
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s