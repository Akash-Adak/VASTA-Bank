apiVersion: apps.kruise.io/v1beta1
kind: CloneSet
metadata:
  name: notification
  namespace: vasta
spec:
  replicas: 6

  selector:
    matchLabels:
      app: notification

  template:
    metadata:
      labels:
        app: notification
    spec:
      containers:
        - name: notification
          image: akta2910/vasta-notify:v1.0.3
          envFrom:
            - configMapRef:
                name: vasta-config
            - secretRef:
                name: vasta-secret
          ports:
            - name: http
            - containerPort: 8085
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

  updateStrategy:
    type: InPlaceIfPossible   # ✅ FIXED HERE

    rollingUpdate:
      partition: 4
      maxUnavailable: 1
      maxSurge: 1

---
apiVersion: v1
kind: Service
metadata:
  name: notification
  namespace: vasta
spec:
  selector:
    app: notification
  ports:

    - port: 8085
      targetPort: 8085